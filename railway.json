{
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "chmod -R 755 storage && chmod -R 755 public && php artisan storage:link && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}
