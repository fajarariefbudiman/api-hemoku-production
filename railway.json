{
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "php artisan config:cache && php artisan rollback && php artisan migrate && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}
