{
    "build": {
        "builder": "nixpacks"
    },
    "deploy": {
        "startCommand": "rm -rf public/storage && php artisan config:cache && php artisan migrate --force && php artisan storage:link && php -S 0.0.0.0:$PORT -t public"
    }
}
